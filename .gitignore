# ----- acoustic branch
.cache/
.venv/
outputs/embeddings/
# Per-call fusion scores: a derivative of the confidential dataset, and regenerable in ~60 s with
# `python src/evaluate_fusion.py`. The behaviour branch keeps per-call caches out of Git; so do we.
outputs/fusion/
__pycache__/
**/__pycache__/
.pytest_cache/
.ruff_cache/
*.pyc
*.log
*.safetensors

# ----- shared: the dataset never lives in the repository
audio/
*.zip
.DS_Store
.env
.env.*
!.env.example

# ----- behaviour branch (its own rules stay scoped in behaviour/.gitignore)
.bootstrap/
.python/
.venv-runtime/
